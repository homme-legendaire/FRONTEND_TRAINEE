import styles from "./Overview.module.css";

const Overview = () => {
  return (
    <div className={styles.mainContainer}>
      <label className={styles.title}>Overview</label>
    </div>
  );
};

export default Overview;
